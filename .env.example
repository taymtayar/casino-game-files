# Copy this file to `.env` (which dotenv loads automatically) before running the server.
# These defaults work with the local Docker Redis + mock operator described in the handover doc.

# Application Configuration
PORT=3000

# Redis Configuration
# Default local connection, no password
REDIS_URL=redis://localhost:6379
